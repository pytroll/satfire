TODO:

* NWC SAF cloud mask
  - filename in message

* saving
  - fileformat(s)?

* Calculate exact observation times for each scanline

TESTING:
* Add night-time test data + test cases
* Add unittests for utils.read_sat_data
* Add unittests for utils.check_static_masks()
* Add unittests for utils.get_footprint_data()
* Add unittests for utils.check_globcover()
* Add unittests for utils.get_close_idxs()
* Add unittests for forest_fire.ForestFire.check_static_masks()
* Add unittests for forest_fire.ForestFire.run()
* Add unittests for forest_fire.ForestFire.collect_sat_data()
* Add unittests for forest_fire.ForestFire.save_text()
* Add unittests for forest_fire.ForestFire.get_land_cover_mask()
* Add unittests for forest_fire.ForestFire.get_snow_mask()
* Add info dictionary to test data
